apiVersion: v1
kind: Service
metadata:
  name: terraforge-app-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: terraforge-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
